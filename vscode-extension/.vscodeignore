# Source and build artefacts not needed at runtime
src/
tsconfig.json
.eslintrc*
.editorconfig
.gitignore
*.map

# Dev tooling (node_modules is intentionally NOT excluded so vsce bundles
# production deps like vscode-languageclient into the VSIX)
.vscode-test/
*.vsix
